# Портфолио

Персональный сайт-портфолио продуктового дизайнера. Свёрстан по макету Figma
(файл `Auth Center`, страница **Final**).

Сейчас готова **главная страница**: первый экран, карточки кейсов и футер с контактами.
Отдельные страницы кейсов — следующий этап.

## Стек

- [Astro](https://astro.build) — статическая генерация, без клиентского JS на этой странице
- Manrope через встроенный в Astro Fonts API: шрифт скачивается при сборке и
  раздаётся со своего домена, запросов к Google на проде нет
- Content collections — кейсы описаны файлами в `src/content/cases/`

## Команды

```bash
npm install       # установить зависимости
npm run dev       # локальная разработка на http://localhost:4321
npm run build     # сборка в dist/
npm run preview   # посмотреть собранный сайт
npm run check     # проверка типов и шаблонов
```

## Что нужно заполнить

В `src/data/profile.ts` лежат заглушки — без них кнопки ведут в никуда:

- `links.telegram` — адрес в Telegram
- `links.linkedin` — профиль LinkedIn
- `links.cv` — путь к PDF резюме; сам файл положить в `public/`

## Структура

```
src/
  styles/tokens.css   # дизайн-токены из переменных Figma: цвета, типографика, сетка
  styles/global.css   # сброс и базовая типографика
  layouts/            # каркас страницы: <head>, шапка, футер
  components/         # Header, Footer, Hero, CaseCard, Button, Badge, Icon…
  content/cases/      # кейсы: по файлу на кейс
  data/profile.ts     # имя, опыт, ссылки
  data/ui.ts          # подписи интерфейса
  pages/index.astro   # главная
public/               # favicon, robots.txt, сюда же резюме
```

### Как добавить кейс

Создать файл в `src/content/cases/`, картинки положить в `src/assets/`:

```yaml
---
title: Название кейса
order: 3 # порядок на главной, меньше — выше
metrics:
  - value: +12%
    label: к конверсии
tags:
  - mobile
shots:
  - src: ../../assets/имя-файла.png
    alt: Что на экране
---
```

Карточка на главной появится сама. Поля проверяются схемой в `src/content.config.ts` —
при опечатке сборка упадёт с понятной ошибкой.

## Ограничения текущего этапа

- **Только десктоп.** Вёрстка рассчитана на ширину 1360px, как в макете; на узких
  экранах появляется горизонтальная прокрутка. Ограничение задано одной строкой
  `min-width` в `src/styles/global.css` — его снятие и есть начало работы над адаптивом.
- **Только русский.** Маршруты под английскую версию заложены в `astro.config.mjs`
  (русский в корне, английский встанет на `/en/`), подписи интерфейса собраны
  в `src/data/ui.ts`, но переводов пока нет.
- Вторая карточка кейса — заготовка: заголовок настоящий, экраны взяты от первого
  кейса. Помечена `placeholder: true`.

## Деплой

Сайт живёт на [Vercel](https://vercel.com): **https://tolegenaibek.vercel.app**

Продакшн обновляется автоматически при пуше в `main`. Пуш в любую другую ветку
даёт отдельную preview-ссылку, на прод не влияющую.

Адрес сайта прописан в двух местах — в `astro.config.mjs` (константа `site`)
и в `public/robots.txt`. При смене домена поправить оба: от `site` зависят
canonical-ссылки, OG-теги и `sitemap`.
